Run:
    This project runs using Python flask. Install flask using pip ("pip install flask"). Then run using "flask run --host=0.0.0.0 --port=[port#]".
    You can then access the site at localhost:[port#]

What it does:
    Stores username/passwords in database when a user registers.
    Then a user can log in with those credentials.